version: '3.8'
services:
  main:
    restart: always
    env_file:
      - ./.env
    build:
      context: .
      target: develop-stage
      dockerfile: ./dockerfile
    volumes:
      - storage:/app/storage
volumes:
  storage: 