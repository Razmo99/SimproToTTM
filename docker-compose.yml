version: '3.8'
services:
  main:
    restart: On-failure
    env_file:
      - ./.env
    build:
      context: .
      #target: build-stage
      dockerfile: ./dockerfile
    volumes:
      - storage:/app/storage
volumes:
  storage: 